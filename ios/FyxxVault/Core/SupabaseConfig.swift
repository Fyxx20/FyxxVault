import Foundation

enum SupabaseConfig {
    // These are PUBLIC values — Row Level Security protects the data
    // Replace with your actual Supabase project values
    static let url = "https://YOUR_PROJECT.supabase.co"
    static let anonKey = "YOUR_ANON_KEY"
}
